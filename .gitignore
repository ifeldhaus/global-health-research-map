# Secrets
.env
.env.*
!.env.example

# Database
*.duckdb
*.duckdb.wal
# Slim database is committed for the public Streamlit Community Cloud deploy
!data/global_health_slim.duckdb

# Large data files
data/raw/*.jsonl
data/processed/*.parquet

# GBD data (downloaded from IHME, not versioned)
data/gbd/

# Python
__pycache__/
*.pyc
.venv/

# Agents (generated output, not versioned)
agents/

# Local tooling / intermediate artifacts (not versioned)
.claude/
data/labels_snapshot_pre_reclass.csv

# Generated figures and docs
docs/figures/
docs/findings_draft.md
docs/findings_data.json
docs/content_drafts.md

# Validation outputs (generated, not versioned)
validation/validation_sample.csv
validation/validation_sample_key.csv
validation/validation_sample_labeled.csv
validation/confusion_matrices/
# Superseded validation tooling and backups, kept locally only
validation/archive/

# OS
.DS_Store

# Subscription classification batches (generated)
batches/

# Paper writing archive — local only, never committed
paper/

# Validation: keep only the reproducible scripts in git; hand-labeled data,
# labeling-tool HTML instances, and per-round docs stay local.
validation/*
!validation/make_holdout_sample.py
!validation/make_labeling_tool.py
!validation/compute_kappa.py

# Bulletin submission manuscript — local only, never committed (no public preprint)
bulletin-who/
